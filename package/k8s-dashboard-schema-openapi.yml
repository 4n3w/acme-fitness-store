openapi: 3.0.0
info:
  version: 0.1.0
  title: Schema for data values, generated by ytt
paths: {}
components:
  schemas:
    dataValues:
      type: object
      additionalProperties: false
      properties:
        target_namespace:
          type: string
          description: Namespace to install into
          default: kubernetes-dashboard
        cluster_role:
          type: string
          description: Cluster role for the kubernetes service acccount
          default: kubernetes-dashboard
        dashboard_domain:
          type: string
          description: domain dashboard is hosted on
          default: https://dashboard.example.com
