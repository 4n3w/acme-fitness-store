apiVersion: kctrl.carvel.dev/v1alpha1
kind: PackageBuild
metadata:
  creationTimestamp: null
  name: kctrl-petclinic.gtt.tanzu.vmware.com
spec:
  release:
  - resource: {}
  template:
    spec:
      app:
        spec:
          deploy:
          - kapp: {}
          template:
          - ytt:
              paths:
              - config
          - kbld: {}
      export:
      - imgpkgBundle:
          image: projects.registry.vmware.com/gtt/packages/kctrl-petclinic
          useKbldImagesLock: true
        includePaths:
        - config
